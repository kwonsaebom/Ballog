const community_error = {
    get: async (error) => {
            console.error('게시글 목록 불러오기 실패', error)
    },
    post: async (error) => {
        console.error('게시글 목록 불러오기 실패', error)
    },
    patch: async (error) => {
        console.error('게시글 목록 불러오기 실패', error)
    },
    delete: async (error) => {
        console.error('게시글 목록 불러오기 실패', error)
    },
    get_list: async (error) => {
        console.error('게시글 목록 불러오기 실패', error)
    }
}